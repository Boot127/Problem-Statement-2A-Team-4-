PORT=5000
CLIENT_ORIGIN=http://localhost:5173

# Dev 2 (Work Permit Management) uses a local SQLite file for now; leave
# unset to default to server/database/hrckmp.db.
SQLITE_DB_PATH=

# MySQL vars below are for the eventual shared database (not used by the
# SQLite-backed Work Permit feature yet).
DB_HOST=localhost
DB_PORT=3306
DB_USER=
DB_PASSWORD=
DB_NAME=hrckmp
JWT_SECRET=
JWT_EXPIRES_IN=1d
AI_API_KEY=
