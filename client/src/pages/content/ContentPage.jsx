// Developer 1 — Compliance Content Management entry point.
// Renders the record list; create/edit/detail live at /content/new, /content/:id, /content/:id/edit.
import RecordListPage from './RecordListPage';

export default function ContentPage() {
  return <RecordListPage />;
}
