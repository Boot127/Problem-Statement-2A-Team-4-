# Optional. vite.config.js already proxies /api and /uploads to
# http://localhost:5000 for local dev, so this isn't required — only set it
# if the server runs somewhere other than localhost:5000.
VITE_API_BASE_URL=http://localhost:5000/api/v1
